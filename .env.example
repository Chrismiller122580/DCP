# Root / shared
# Copy to .env for local tooling if needed

# API specific (also in packages/api/.env or loaded via ConfigModule)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/dcp?schema=public"
XRPL_NETWORK=testnet
PORT=4000
